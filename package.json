{
  "name": "platform-monorepo",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "devDependencies": {
    "turbo": "^2.0.5",
    "typescript": "^5.5.4",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier -w ."
  }
}
