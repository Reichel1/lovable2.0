{
  "name": "platform-monorepo",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "turbo": "^2.0.5",
    "typescript": "^5.5.4",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier -w ."
  }
}
